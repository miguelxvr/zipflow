# ============================================================
# zipflow Configuration
# ============================================================
# Configure source and target using URI schemes
# Uncomment ONE of the examples below

# ------------------------------------------------------------
# Example 1: Filesystem Provider (Local Testing)
# ------------------------------------------------------------
SOURCE_URI=file://./storage/input
TARGET_URI=file://./storage/output/archive.zip
COMPRESSION_LEVEL=9

# ------------------------------------------------------------
# Example 2: AWS S3 (Production)
# ------------------------------------------------------------
# SOURCE_URI=s3://my-source-bucket/data/to-archive/
# TARGET_URI=s3://my-target-bucket/archives/output.zip
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# COMPRESSION_LEVEL=9

# ------------------------------------------------------------
# Example 3: MinIO (Local S3 Testing)
# ------------------------------------------------------------
# SOURCE_URI=s3://test-bucket/data/
# TARGET_URI=s3://test-bucket/archives/result.zip
# AWS_ENDPOINT_URL=http://localhost:9000
# AWS_ACCESS_KEY_ID=minioadmin
# AWS_SECRET_ACCESS_KEY=minioadmin
# AWS_REGION=us-east-1
# S3_FORCE_PATH_STYLE=true
# COMPRESSION_LEVEL=9

# ------------------------------------------------------------
# Optional Settings
# ------------------------------------------------------------
# UPLOAD_PART_SIZE=5242880
# UPLOAD_QUEUE_SIZE=1
# UPLOAD_TIMEOUT=3600000
